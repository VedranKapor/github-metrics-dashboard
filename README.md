# CCG GitHub Metrics Dashboard

This repository automatically aggregates GitHub statistics for selected repositories in the CCG / OSeMOSYS ecosystem.

Metrics are saved into: `github_metrics.csv`

Run locally:

```
python3 aggregate_github_metrics.py
```

Add repositories to `repos.txt`.
